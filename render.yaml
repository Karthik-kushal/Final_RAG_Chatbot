services:
  - type: web
    name: rag-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/server.py"
    envVars:
      - key: GROQ_API_KEY
        scope: build
      - key: PYTHONUNBUFFERED
        value: "1"
